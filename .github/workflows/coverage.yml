name: Coverage

on:
  workflow_run:
    workflows:
      - build-and-test
    types:
      - completed
    branches:
      - main

permissions: {}

concurrency:
  group: coverage-pages
  cancel-in-progress: true

jobs:
  coverage:
    permissions:
      contents: read
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main' &&
      github.event.workflow_run.head_sha == github.sha
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Cache NuGet packages
        uses: actions/cache@v5
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/*.slnx') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore SwiftCollections.slnx --property:Configuration=Release

      - name: Build solution
        run: dotnet build SwiftCollections.slnx --configuration Release --no-restore

      - name: Run tests with coverage
        run: dotnet test tests/SwiftCollections.Tests/SwiftCollections.Tests.csproj --configuration Release --no-build --collect:"XPlat Code Coverage" --settings tests/SwiftCollections.Tests/coverlet.runsettings --results-directory artifacts/coverage

      - name: Restore local tools
        run: dotnet tool restore

      - name: Build API documentation
        run: |
          test -f src/SwiftCollections/bin/Release/net8.0/SwiftCollections.dll
          test -f src/SwiftCollections/bin/Release/net8.0/SwiftCollections.xml
          test -f src/SwiftCollections.FixedMathSharp/bin/Release/net8.0/SwiftCollections.FixedMathSharp.dll
          test -f src/SwiftCollections.FixedMathSharp/bin/Release/net8.0/SwiftCollections.FixedMathSharp.xml
          dotnet tool run docfx docs/api/docfx.json
          test -f docs/api/obj/_site/index.html
          test -f docs/api/obj/_site/api/SwiftCollections.html
          test -f docs/api/obj/_site/api/SwiftCollections.Query.html

      - name: Install ReportGenerator
        run: dotnet tool install --global dotnet-reportgenerator-globaltool

      - name: Add ReportGenerator to PATH
        run: echo "$HOME/.dotnet/tools" >> "$GITHUB_PATH"

      - name: Generate coverage report
        run: |
          reportgenerator \
            "-reports:artifacts/coverage/**/coverage.cobertura.xml" \
            "-targetdir:artifacts/coverage-report" \
            "-reporttypes:Html;MarkdownSummaryGithub;Badges;JsonSummary" \
            "-filefilters:-**/MemoryPack.Generator/**;-**/*.g.cs;-**/obj/**"

      - name: Add coverage summary to job
        run: cat artifacts/coverage-report/SummaryGithub.md >> "$GITHUB_STEP_SUMMARY"

      - name: Assemble Pages site
        run: |
          mkdir -p artifacts/pages/coverage
          cp -a docs/api/obj/_site/. artifacts/pages/
          cp -a artifacts/coverage-report/. artifacts/pages/coverage/
          test -f artifacts/pages/index.html
          test -f artifacts/pages/api/SwiftCollections.html
          test -f artifacts/pages/api/SwiftCollections.Query.html
          test -f artifacts/pages/coverage/Summary.json
          test -f artifacts/pages/coverage/badge_branchcoverage.svg

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: artifacts/coverage-report

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: artifacts/pages

  deploy:
    needs: coverage
    permissions:
      contents: read
      pages: write
      id-token: write
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
